#ifndef UTILS_H
#define UTILS_H

// Fungsi untuk mengkonversi koordinat 2D menjadi koordinat isometrik
int isoX(int x, int y, int tileWidth, int tileHeight);
int isoY(int x, int y, int tileWidth, int tileHeight);

#endif
